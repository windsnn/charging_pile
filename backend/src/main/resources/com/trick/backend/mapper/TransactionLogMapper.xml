<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.trick.backend.mapper.TransactionLogMapper">

    <insert id="addLogT">
        insert into transaction_log(transaction_no, user_id, order_id, amount, type,
                                    status, description, create_time, update_time)
        values (#{transactionNo}, #{userId}, #{orderId}, #{amount}, #{type},
                #{status}, #{description}, #{createTime}, #{updateTime})
    </insert>

    <select id="getAllTransactions" resultType="com.trick.backend.model.pojo.TransactionLog">
        select id, transaction_no, amount, type, status, create_time, description
        from transaction_log
        where user_id = #{id}
        order by id desc
    </select>

</mapper>